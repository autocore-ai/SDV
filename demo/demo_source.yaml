flow: RandomGeneration
operators: []
sources:
  - id : DemoSource
    uri: file://./target/debug/libdemo_source.so
    output:
        id: input
        type: usize
sinks:
  - id : DemoSink
    uri: file://./target/debug/libdemo_sink.so
    input:
        id: output
        type: usize
links:
- from: DemoSource.input
  to: DemoSink.output
